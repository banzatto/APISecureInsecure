# InsecureAPI (OWASP Demo - Vulnerable)

Projeto ASP.NET Core (net8.0) com endpoints **vulneráveis** ilustrando as falhas do OWASP Top10 + API Top10.

## Como rodar

Requer .NET 8 SDK.

```bash
cd InsecureAPI
dotnet restore
dotnet run --urls http://localhost:5002
